[package]
name = "rpc"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../../shared" }

corepc-client = { version = "0.8.0", features = ["client-sync"]}

[features]
# Treat warnings as a build error.
strict = []
